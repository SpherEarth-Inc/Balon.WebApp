DirectoryIndex index.html

# HTML pages should revalidate so browsers pick up new builds after deploy.
# /_next/static/* uses content hashes → safe to cache long-term.
<IfModule mod_headers.c>
  <FilesMatch "\.html$">
    Header set Cache-Control "no-cache, private, must-revalidate"
  </FilesMatch>

  SetEnvIf Request_URI "^/_next/static/.*" SPHEREARTH_NEXT_STATIC=1
  Header set Cache-Control "public, max-age=31536000, immutable" env=SPHEREARTH_NEXT_STATIC
</IfModule>

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Legacy parent page URLs
  RewriteRule ^parent-consultation/?$ /parents-and-families/consultation/ [R=301,L]
  RewriteRule ^parents/?$ /parents-and-families/ [R=301,L]

  # Redirect non-trailing-slash URLs to trailing slash (matches Next.js trailingSlash: true)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_URI} !/$
  RewriteRule ^(.+)$ /$1/ [R=301,L]

  # Dynamic news articles: serve the static-export placeholder "_" page.
  # Client React still reads the real slug from the browser URL.
  # Skip existing files so /news/ (news/index.html) is not stolen into news/_/.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^news/[^/]+/?$ /news/_/index.html [L]

  # Serve folder index.html for clean URLs
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME}/index.html -f
  RewriteRule ^(.+?)/?$ $1/index.html [L]
</IfModule>

# Redeploy bump 2026-07-29-news-rewrite-fix
